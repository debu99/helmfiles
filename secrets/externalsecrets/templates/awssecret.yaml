apiVersion: v1
kind: Secret
metadata:
  name: aws-secret
  namespace: {{ .Values.namespace }}
type: Opaque
data:
  aws-access-key: {{ .Values.aws_access_key }}
  aws-secret-key: {{ .Values.aws_secret_key }}
